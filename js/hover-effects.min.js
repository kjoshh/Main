let monitorTerminalState;document.addEventListener("DOMContentLoaded",function(){console.log("hover-effects.js: DOMContentLoaded");let hoverEffectActive=false;let hoverEventHandler;let throttledHoverEventHandler;let userHoverDisabled=false;function throttle(func,limit){let inThrottle;return function(...args){const context=this;if(!inThrottle){func.apply(context,args);inThrottle=true;setTimeout(()=>inThrottle=false,limit)}}}function initializeHoverScript(){console.log("hover-effects.js: initializeHoverScript() called");if(!hoverEffectActive)return;const hoveredLinksQueue=[];let isHovering=false;let lastHoveredLink=null;hoverEventHandler=event=>{if(!hoverEffectActive)return;const mouseX=event.clientX;const mouseY=event.clientY;const links=document.querySelectorAll(".link");let closestLink=null;let smallestDistance=Infinity;links.forEach(link=>{const rect=link.getBoundingClientRect();const linkCenterX=(rect.left+rect.right)/2;const linkCenterY=(rect.top+rect.bottom)/2;const distance=Math.sqrt(Math.pow(mouseX-linkCenterX,2)+Math.pow(mouseY-linkCenterY,2));if(distance<smallestDistance){smallestDistance=distance;closestLink=link}});if(closestLink&&closestLink!==lastHoveredLink){hoveredLinksQueue.push(closestLink);lastHoveredLink=closestLink;processQueue()}};throttledHoverEventHandler=throttle(hoverEventHandler,50);document.addEventListener("mousemove",throttledHoverEventHandler);function processQueue(){if(!hoverEffectActive||isHovering||hoveredLinksQueue.length===0)return;isHovering=true;const link=hoveredLinksQueue.shift();link.dispatchEvent(new MouseEvent("mouseover",{bubbles:true}));setTimeout(()=>{link.dispatchEvent(new MouseEvent("mouseout",{bubbles:true}));isHovering=false;processQueue()},80)}}function stopHoverScript(){if(hoverEventHandler){document.removeEventListener("mousemove",throttledHoverEventHandler);hoverEventHandler=null}}document.addEventListener("hoverEffectsReady",function(){console.log("hover-effects.js: hoverEffectsReady event received");hoverEffectActive=true;initializeHoverScript()});const links=document.querySelectorAll("a");links.forEach(function(link){link.addEventListener("click",function(event){const href=this.getAttribute("href");stopHoverScript();hoverEffectActive=false;userHoverDisabled=true;setTimeout(()=>{if(!window.terminalActive&&userHoverDisabled){hoverEffectActive=true;userHoverDisabled=false;initializeHoverScript()}},750)})});window.addEventListener("beforeunload",()=>{clearInterval(monitorTerminalState)})});